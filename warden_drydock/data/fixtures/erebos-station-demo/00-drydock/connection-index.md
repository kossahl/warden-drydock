# Connection Index

> Generated by Warden Drydock. Backlinks are derived; edit source records instead.

## [[adventure-erebos-choice|Erebos: Wen nehmen wir mit?]]

- outgoing `involves` → [[deep-relay]] (`current`) — Das Relais verbindet Vermisste, Firma und Echo.
- outgoing `involves` → [[mnemosyne-extraction]] (`current`) — Die Firma setzt die Evakuierungspriorität.
- outgoing `prepares` → [[prep-session-03-wake]] (`planned`) — Die nächste Session setzt mehrere Wege offen.

## [[air-ledger|Gefälschtes Luftprotokoll]]

- No explicit connections.

## [[basalt-larva|Basaltlarve]]

- outgoing `found-at` → [[shaft-six]] (`possible`) — Die Larven sitzen in frischen Bohrspuren.

## [[black-glass-sample|Schwarzglas-Probe]]

- outgoing `held-by` → [[dr-pavel-ix]] (`hidden`) — Ix will die Probe evakuieren.
- outgoing `originated-from` → [[shaft-six]] (`confirmed`) — Die Probe stammt aus der letzten Bohrung.
- backlink `carries` ← [[dr-pavel-ix]] (`hidden`) — Ix trägt die Probe in einem Bleikoffer.
- backlink `knows-about` ← [[dr-sana-okafor]] (`hidden`) — Sie erkennt das Material aus einem alten Auftrag.

## [[campaign-main|Erebos Station Demo]]

- No explicit connections.

## [[canteen|Kantine 2]]

- outgoing `part-of` → [[erebos-station]] (`current`) — Kantine 2 gehört zur Station.
- backlink `starts-at` ← [[prep-session-03-wake]] (`planned`) — Die Totenwache ist die erste Szene.

## [[chief-belen|Chief Aram Belen]]

- No explicit connections.

## [[cold-thruster|Kalte Manöverdüse]]

- outgoing `found-at` → [[sector-c]] (`confirmed`) — Inventar und Rusk bestätigen den Lagerort.
- outgoing `intended-for` → [[kestrel]] (`current`) — Mara kann die Düse einbauen.

## [[comms-spine|Funkspindel]]

- outgoing `part-of` → [[erebos-station]] (`current`) — Funkspindel gehört zur Station.
- backlink `operated-by` ← [[mei-ardon]] (`current`) — Mei arbeitet dort allein.

## [[corporate-arrival|Firmeninspektion trifft ein]]

- No explicit connections.

## [[debrief-after-session-02|Debrief nach Session 2]]

- outgoing `follows` → [[session-02-dead-voice]] (`confirmed`) — Direkte Nachbesprechung.

## [[deep-relay|Tiefenrelais]]

- outgoing `part-of` → [[erebos-station]] (`current`) — Tiefenrelais gehört zur Station.
- backlink `changed` ← [[niko-vale]] (`confirmed`) — Vale schaltete es vor seinem Tod ab.
- backlink `concerns` ← [[relay-is-listening]] (`hidden`) — Die Wirkung geht vom Relaisverbund aus.
- backlink `involves` ← [[adventure-erebos-choice]] (`current`) — Das Relais verbindet Vermisste, Firma und Echo.
- backlink `originated-from` ← [[echo-host]] (`hidden`) — Der Wirt entstand nach Kontakt mit dem Relais.
- backlink `uses` ← [[vale-key]] (`confirmed`) — Der Schlüssel passt zur Servicetür.

## [[director-havel|Direktor Orson Havel]]

- outgoing `controls` → [[erebos-station]] (`current`) — Havel kontrolliert Verfahren und Evakuierungslisten.
- outgoing `works-for` → [[mnemosyne-extraction]] (`confirmed`) — Sein Vertrag belohnt gerettete Daten, nicht Menschen.
- backlink `caused-by` ← [[havel-sacrifice]] (`hidden`) — Havel ordnete die falsche Gefahrenmeldung an.
- backlink `controlled-by` ← [[erebos-station]] (`current`) — Havel besitzt die offiziellen Zugriffsschlüssel.
- backlink `employs` ← [[mnemosyne-extraction]] (`confirmed`) — Havel setzt die Evakuierungspriorität um.
- backlink `opposed-to` ← [[union-six]] (`current`) — Die Evakuierungsliste hat den offenen Konflikt ausgelöst.

## [[dock-ring|Dockring]]

- outgoing `part-of` → [[erebos-station]] (`current`) — Dockring gehört zur Station.
- backlink `changed` ← [[session-01-dockfire]] (`confirmed`) — Dockarm 3 wurde zerstört.
- backlink `located-at` ← [[grey-pilgrim]] (`current`) — Grey Pilgrim ist am oder auf Kurs zum Dockring.
- backlink `located-at` ← [[kestrel]] (`current`) — Kestrel ist am oder auf Kurs zum Dockring.
- backlink `located-at` ← [[morrow]] (`current`) — Morrow ist am oder auf Kurs zum Dockring.

## [[dr-pavel-ix|Dr. Pavel Ix]]

- outgoing `carries` → [[black-glass-sample]] (`hidden`) — Ix trägt die Probe in einem Bleikoffer.
- backlink `held-by` ← [[black-glass-sample]] (`hidden`) — Ix will die Probe evakuieren.

## [[dr-sana-okafor|Dr. Sana Okafor]]

- outgoing `knows-about` → [[black-glass-sample]] (`hidden`) — Sie erkennt das Material aus einem alten Auftrag.
- outgoing `performed-by` → [[vale-autopsy]] (`confirmed`) — Sana bestimmte Todesursache und Zeitfenster.

## [[echo-host|Echo-Wirt]]

- outgoing `located-at` → [[sector-c]] (`hidden`) — Das Klopfen kommt von einem eingeschlossenen Wirt.
- outgoing `originated-from` → [[deep-relay]] (`hidden`) — Der Wirt entstand nach Kontakt mit dem Relais.
- backlink `threatened-by` ← [[ilya-ross]] (`unknown`) — Ilya hörte als Erster das Klopfen in Sektor C.

## [[erebos-station|Erebos Station]]

- outgoing `controlled-by` → [[director-havel]] (`current`) — Havel besitzt die offiziellen Zugriffsschlüssel.
- outgoing `threatened-by` → [[oxygen-collapse]] (`active`) — Die gefälschten Werte verdecken ein echtes Verteilungsproblem.
- backlink `affects` ← [[station-abandoned]] (`possible`) — Die Station wird versiegelt oder driftet ohne Wartung.
- backlink `controls` ← [[director-havel]] (`current`) — Havel kontrolliert Verfahren und Evakuierungslisten.
- backlink `owns` ← [[mnemosyne-extraction]] (`confirmed`) — Die Firma beansprucht Station und Funde.
- backlink `part-of` ← [[canteen]] (`current`) — Kantine 2 gehört zur Station.
- backlink `part-of` ← [[comms-spine]] (`current`) — Funkspindel gehört zur Station.
- backlink `part-of` ← [[deep-relay]] (`current`) — Tiefenrelais gehört zur Station.
- backlink `part-of` ← [[dock-ring]] (`current`) — Dockring gehört zur Station.
- backlink `part-of` ← [[sector-c]] (`current`) — Sektor C gehört zur Station.
- backlink `part-of` ← [[shaft-six]] (`current`) — Schacht 6 gehört zur Station.

## [[erin-sato|Erin Sato]]

- No explicit connections.

## [[father-salt|Salvatore 'Father' Kade]]

- No explicit connections.

## [[grey-pilgrim|Grey Pilgrim]]

- outgoing `located-at` → [[dock-ring]] (`current`) — Grey Pilgrim ist am oder auf Kurs zum Dockring.

## [[havel-sacrifice|Havel opfert Sektor C]]

- outgoing `applies-to` → [[sector-c]] (`confirmed`) — Die Abriegelung betrifft den gesamten Sektor.
- outgoing `caused-by` → [[director-havel]] (`hidden`) — Havel ordnete die falsche Gefahrenmeldung an.

## [[ilya-ross|Ilya Ross]]

- outgoing `allied-with` → [[selene-rusk]] (`current`) — Ilya gab Rusk sein Wort.
- outgoing `threatened-by` → [[echo-host]] (`unknown`) — Ilya hörte als Erster das Klopfen in Sektor C.

## [[juno-quell|Juno Quell]]

- backlink `threatened-by` ← [[teo-malik]] (`hidden`) — Der private Bergungsvertrag und Teos Schulden laufen über Quell.

## [[kestrel|Kestrel]]

- outgoing `located-at` → [[dock-ring]] (`current`) — Kestrel ist am oder auf Kurs zum Dockring.
- backlink `affects` ← [[kestrel-blacklisted]] (`possible`) — Die Schiffkennung wird zum Risiko.
- backlink `intended-for` ← [[cold-thruster]] (`current`) — Mara kann die Düse einbauen.
- backlink `operated-by` ← [[mara-venn]] (`current`) — Mara hält das beschädigte Schiff am Leben.
- backlink `operated-by` ← [[teo-malik]] (`current`) — Teo ist der Pilot.

## [[kestrel-blacklisted|Kestrel wird gesperrt]]

- outgoing `affects` → [[kestrel]] (`possible`) — Die Schiffkennung wird zum Risiko.
- outgoing `caused-by` → [[mnemosyne-extraction]] (`possible`) — Die Firma kann die Sperre auslösen.

## [[lem-ortega|Lem Ortega]]

- No explicit connections.

## [[mara-venn|Mara Venn]]

- outgoing `concerns` → [[vale-helmet-recording]] (`hidden`) — Eine Stimme mit Maras Muster ist auf der Aufnahme.
- outgoing `operated-by` → [[kestrel]] (`current`) — Mara hält das beschädigte Schiff am Leben.
- backlink `features` ← [[session-01-dockfire]] (`confirmed`) — Mara hielt das Feuer von der Kestrel fern.

## [[mei-ardon|Mei Ardon]]

- outgoing `knows-about` → [[third-reply]] (`hidden`) — Sie löschte einen Teil der empfangenen Nachricht.
- outgoing `operated-by` → [[comms-spine]] (`current`) — Mei arbeitet dort allein.

## [[mnemosyne-extraction|Mnemosyne Extraction]]

- outgoing `employs` → [[director-havel]] (`confirmed`) — Havel setzt die Evakuierungspriorität um.
- outgoing `owns` → [[erebos-station]] (`confirmed`) — Die Firma beansprucht Station und Funde.
- backlink `caused-by` ← [[kestrel-blacklisted]] (`possible`) — Die Firma kann die Sperre auslösen.
- backlink `involves` ← [[adventure-erebos-choice]] (`current`) — Die Firma setzt die Evakuierungspriorität.
- backlink `works-for` ← [[director-havel]] (`confirmed`) — Sein Vertrag belohnt gerettete Daten, nicht Menschen.

## [[morrow|Morrow]]

- outgoing `located-at` → [[dock-ring]] (`current`) — Morrow ist am oder auf Kurs zum Dockring.

## [[niko-vale|Niko Vale]]

- outgoing `changed` → [[deep-relay]] (`confirmed`) — Vale schaltete es vor seinem Tod ab.
- outgoing `source-of` → [[vale-helmet-recording]] (`confirmed`) — Die Aufnahme stammt aus seinem Anzug.
- backlink `owned-by` ← [[vale-key]] (`former`) — Vale trug ihn beim Tod.

## [[oskar-jinn|Oskar Jinn]]

- No explicit connections.

## [[oxygen-collapse|Luftreserve bricht zusammen]]

- backlink `threatened-by` ← [[erebos-station]] (`active`) — Die gefälschten Werte verdecken ein echtes Verteilungsproblem.

## [[pilot-chen|Lin Chen]]

- No explicit connections.

## [[prep-session-03-wake|Session 3: Totenwache]]

- outgoing `follows` → [[session-02-dead-voice]] (`confirmed`) — Beginnt wenige Stunden später.
- outgoing `starts-at` → [[canteen]] (`planned`) — Die Totenwache ist die erste Szene.
- backlink `prepares` ← [[adventure-erebos-choice]] (`planned`) — Die nächste Session setzt mehrere Wege offen.

## [[relay-awakening|Relais erwacht]]

- No explicit connections.

## [[relay-is-listening|Das Relais hört voraus]]

- outgoing `concerns` → [[deep-relay]] (`hidden`) — Die Wirkung geht vom Relaisverbund aus.
- outgoing `revealed` → [[vale-helmet-recording]] (`possible`) — Die Aufnahme ist der erste harte Widerspruch.

## [[sector-c|Sektor C]]

- outgoing `part-of` → [[erebos-station]] (`current`) — Sektor C gehört zur Station.
- backlink `applies-to` ← [[havel-sacrifice]] (`confirmed`) — Die Abriegelung betrifft den gesamten Sektor.
- backlink `found-at` ← [[cold-thruster]] (`confirmed`) — Inventar und Rusk bestätigen den Lagerort.
- backlink `located-at` ← [[echo-host]] (`hidden`) — Das Klopfen kommt von einem eingeschlossenen Wirt.
- backlink `located-at` ← [[selene-rusk]] (`former`) — Sie wurde aus dem Aufzug an der Sperre geborgen.

## [[selene-rusk|Selene Rusk]]

- outgoing `located-at` → [[sector-c]] (`former`) — Sie wurde aus dem Aufzug an der Sperre geborgen.
- outgoing `works-for` → [[union-six]] (`confirmed`) — Rusk vertritt die Bergleute.
- backlink `allied-with` ← [[ilya-ross]] (`current`) — Ilya gab Rusk sein Wort.
- backlink `allied-with` ← [[union-six]] (`confirmed`) — Rusk spricht für die Schicht.

## [[session-01-dockfire|Session 1: Feuer am Dock]]

- outgoing `changed` → [[dock-ring]] (`confirmed`) — Dockarm 3 wurde zerstört.
- outgoing `features` → [[mara-venn]] (`confirmed`) — Mara hielt das Feuer von der Kestrel fern.

## [[session-02-dead-voice|Session 2: Die Stimme des Toten]]

- outgoing `revealed` → [[vale-autopsy]] (`confirmed`) — Der Todeszeitpunkt widerspricht Havel.
- outgoing `revealed` → [[vale-helmet-recording]] (`confirmed`) — Die Crew besitzt eine Kopie.
- backlink `follows` ← [[debrief-after-session-02]] (`confirmed`) — Direkte Nachbesprechung.
- backlink `follows` ← [[prep-session-03-wake]] (`confirmed`) — Beginnt wenige Stunden später.

## [[shaft-six|Schacht 6]]

- outgoing `part-of` → [[erebos-station]] (`current`) — Schacht 6 gehört zur Station.
- backlink `found-at` ← [[basalt-larva]] (`possible`) — Die Larven sitzen in frischen Bohrspuren.
- backlink `originated-from` ← [[black-glass-sample]] (`confirmed`) — Die Probe stammt aus der letzten Bohrung.

## [[shift-map|Jinns Schichtkarte]]

- No explicit connections.

## [[silent-distress|Havels stilles Notsignal]]

- No explicit connections.

## [[station-abandoned|Erebos wird aufgegeben]]

- outgoing `affects` → [[erebos-station]] (`possible`) — Die Station wird versiegelt oder driftet ohne Wartung.

## [[teo-malik|Teo Malik]]

- outgoing `operated-by` → [[kestrel]] (`current`) — Teo ist der Pilot.
- outgoing `threatened-by` → [[juno-quell]] (`hidden`) — Der private Bergungsvertrag und Teos Schulden laufen über Quell.

## [[third-reply|Die dritte Antwort]]

- backlink `knows-about` ← [[mei-ardon]] (`hidden`) — Sie löschte einen Teil der empfangenen Nachricht.

## [[union-six|Schichtkollektiv 6]]

- outgoing `allied-with` → [[selene-rusk]] (`confirmed`) — Rusk spricht für die Schicht.
- outgoing `opposed-to` → [[director-havel]] (`current`) — Die Evakuierungsliste hat den offenen Konflikt ausgelöst.
- backlink `works-for` ← [[selene-rusk]] (`confirmed`) — Rusk vertritt die Bergleute.

## [[vale-autopsy|Vales Autopsie]]

- backlink `performed-by` ← [[dr-sana-okafor]] (`confirmed`) — Sana bestimmte Todesursache und Zeitfenster.
- backlink `revealed` ← [[session-02-dead-voice]] (`confirmed`) — Der Todeszeitpunkt widerspricht Havel.

## [[vale-helmet-recording|Vales Helmaufnahme]]

- backlink `concerns` ← [[mara-venn]] (`hidden`) — Eine Stimme mit Maras Muster ist auf der Aufnahme.
- backlink `revealed` ← [[relay-is-listening]] (`possible`) — Die Aufnahme ist der erste harte Widerspruch.
- backlink `revealed` ← [[session-02-dead-voice]] (`confirmed`) — Die Crew besitzt eine Kopie.
- backlink `source-of` ← [[niko-vale]] (`confirmed`) — Die Aufnahme stammt aus seinem Anzug.

## [[vale-key|Vales Wartungsschlüssel]]

- outgoing `owned-by` → [[niko-vale]] (`former`) — Vale trug ihn beim Tod.
- outgoing `uses` → [[deep-relay]] (`confirmed`) — Der Schlüssel passt zur Servicetür.

## [[workers-mutiny|Arbeiter besetzen die Morrow]]

- No explicit connections.
