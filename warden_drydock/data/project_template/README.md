# {{campaign_name}}

This is a standalone campaign repository generated by **Warden Drydock**.

## Normal interaction

Work through a repository-capable AI agent. Start a new agent session with:

```text
Read AGENTS.md and 00-drydock/ai-context.md before taking action.
Treat Git as the source of truth and do not invent canon.
```

## Deterministic maintenance

An AI agent should use the included scripts:

```bash
python scripts/drydock.py validate
python scripts/drydock.py context
```

## Repository ownership

- Campaign content belongs to this repository.
- The framework is not a Git submodule.
- `.drydock.json` records the generator and adapter versions.
- Framework updates must be applied as reviewable, non-destructive migrations.
