import { useEffect, useMemo, useRef, useState, type FormEvent, type ReactNode } from "react";
import type { AtlasApi } from "../api/atlasClient";
import { httpSliceApi, type SliceApi } from "../api/client";
import type { AtlasCampaignItem, AtlasFacets, AtlasHistoryCollection, AtlasHistoryEntry, AtlasNeighborhood, AtlasOverview, AtlasRecordLibraryResult, AtlasRevisionRef, ProviderReadiness, RawStatus } from "../contracts/v2";
import { SafeMarkdown } from "./SafeMarkdown";
import { bindingMatches, ErrorState, GroundedAiPanel, isCursorFailure, isIntegrityFailure, Link, openHeadHref, RelationshipView, revisionQuery, stateFromRoute, titleCase, useResource, WorkflowPanels, type Navigate, type Resource } from "./AtlasCompletion";
import { atlasHref, parseAtlasRoute, recordHref, type AtlasRoute, type AtlasUrlState } from "./routing";
import { RecordEditor } from "../editor/RecordEditor";

export type ProviderState = ProviderReadiness;

function ProviderStatus({ resource }: { resource: Resource<ProviderState> }) {
  const value = resource.value;
  const label = resource.pending && !value ? "Checking" : !value ? "Unavailable" : !value.provider_configured ? "Setup required" : !value.provider_available ? "Unavailable" : !value.consent_current ? "Consent required" : value.ai_available ? "Ready" : "Unavailable";
  return <span role="status">Provider: {label}</span>;
}

function RecordDetailView(props: { api: AtlasApi; sliceApi: SliceApi; provider: Resource<ProviderState>; campaign: AtlasCampaignItem; route: AtlasRoute; revision: AtlasRevisionRef; navigate: Navigate; block: (error: unknown) => void }) {
  return <><RecordDetailContent {...props} /><RecordEditor campaignId={props.campaign.campaign_id} revisionId={props.revision.revision_id} recordId={props.route.recordId ?? ""} navigate={props.navigate} /></>;
}

function Shell({ campaign, route, viewed, provider, navigate, children }: { campaign: AtlasCampaignItem; route: AtlasRoute; viewed: AtlasRevisionRef; provider: Resource<ProviderState>; navigate: Navigate; children: ReactNode }) {
  const state = stateFromRoute(route, viewed.revision_id); const historical = viewed.revision_id !== campaign.head_revision.revision_id;
  return <div className="app-shell atlas-shell"><a className="skip-link" href="#atlas-content">Skip to main content</a><header className="banner"><div><p className="eyebrow">Local Warden workspace</p><Link href="/" navigate={navigate} className="brand-link">Warden Drydock proposal workspace</Link></div><ProviderStatus resource={provider} /></header><div className="atlas-layout"><aside className="atlas-rail"><p className="atlas-campaign-name">{campaign.campaign_name}</p><nav aria-label="Campaign Atlas"><Link href={atlasHref(campaign.campaign_id, "overview", state)} navigate={navigate} current={route.kind === "overview"}>Overview</Link><Link href={atlasHref(campaign.campaign_id, "records", state)} navigate={navigate} current={route.kind === "records" || route.kind === "record"}>Records</Link><Link href={atlasHref(campaign.campaign_id, "history", state)} navigate={navigate} current={route.kind === "history"}>Approved history</Link></nav></aside><div className="atlas-main-wrap"><aside className="revision-banner" aria-label="Viewed revision"><span>Viewed revision {viewed.ordinal} · <code>{viewed.revision_id}</code> · {historical ? "Historical" : "Head"}</span>{historical && <Link href={openHeadHref(route, campaign.head_revision)} navigate={navigate} className="button-link">Open head</Link>}</aside><main id="atlas-content" className="atlas-main" tabIndex={-1}>{children}</main></div></div></div>;
}

function Heading({ children, focusKey }: { children: ReactNode; focusKey?: string | null }) {
  const heading = useRef<HTMLHeadingElement>(null);
  useEffect(() => {
    if (focusKey === undefined || focusKey === null) return;
    const focusAfterRouteRender = window.setTimeout(() => heading.current?.focus(), 0);
    return () => window.clearTimeout(focusAfterRouteRender);
  }, [focusKey]);
  return <h1 ref={heading} tabIndex={-1}>{children}</h1>;
}
const statusLabel = (status: RawStatus) => status.classification === "known" ? status.value === "accepted" ? "Accepted (legacy)" : titleCase(status.value) : status.classification === "missing" ? "Not recorded" : `Unknown (${status.value})`;
const statusFilterLabel = (value: string) => value === "accepted" ? "Accepted (legacy)" : titleCase(value);
function RecordLabels({ authority, status }: { authority: string; status: RawStatus }) { return <div className="record-labels"><span className={`authority-label authority-label--${authority}`}>Authority: {titleCase(authority)}</span><span className={`status-label status-label--${status.classification}`}>Status: {statusLabel(status)}</span></div>; }

function FacetLinks({ facets, campaignId, revisionId, navigate }: { facets: AtlasFacets; campaignId: string; revisionId: string; navigate: Navigate }) {
  const sections = [["Record type", facets.record_types, "type"], ["Authority", facets.authorities, "authority"], ["Status", facets.statuses, "status"]] as const;
  return <div className="facet-groups">{sections.map(([heading, values, key]) => <section key={key}><h3>{heading}</h3><ul>{values.map((facet) => { const state: AtlasUrlState = { revisionId, [key]: facet.value }; return <li key={facet.value}><Link href={atlasHref(campaignId, "records", state)} navigate={navigate}>{key === "status" ? statusFilterLabel(facet.value) : titleCase(facet.value)} <span>({facet.count})</span></Link></li>; })}</ul></section>)}</div>;
}

function HistoryEntries({ value, campaignId, navigate, compact = false }: { value: AtlasHistoryCollection; campaignId: string; navigate: Navigate; compact?: boolean }) {
  if (!value.entries.length) return <p>No approved revision events are recorded.</p>;
  return <ol className="history-list">{value.entries.map((entry) => <HistoryEntry key={entry.revision.revision_id} entry={entry} campaignId={campaignId} navigate={navigate} compact={compact} />)}</ol>;
}
function HistoryEntry({ entry, campaignId, navigate, compact }: { entry: AtlasHistoryEntry; campaignId: string; navigate: Navigate; compact: boolean }) {
  return <li className="history-entry"><h3>Revision {entry.revision.ordinal}</h3><p><code>{entry.revision.revision_id}</code>{entry.parent_revision_id ? <> · Parent <Link href={atlasHref(campaignId, "overview", { revisionId: entry.parent_revision_id })} navigate={navigate}><code>{entry.parent_revision_id}</code></Link></> : " · Initial revision"}</p>{!compact && <><ul className="change-list">{entry.changes.map((change, index) => <li key={`${change.record_id}-${change.change_kind}-${index}`}><span className={`change-kind change-kind--${change.change_kind}`}>{titleCase(change.change_kind)}</span>{" "}<Link href={recordHref(campaignId, change.record_id, { revisionId: change.link_revision_id })} navigate={navigate}>{change.change_kind === "removed" ? `Removed record ${change.record_id}` : change.record_id}</Link>{change.from_authority !== change.to_authority && <span> · Authority: {change.from_authority ? titleCase(change.from_authority) : "None"} → {change.to_authority ? titleCase(change.to_authority) : "None"}</span>}</li>)}</ul>{entry.proposal_id && entry.proposal_version && <p>Proposal <code>{entry.proposal_id}</code>, version {entry.proposal_version}</p>}</>}</li>;
}

function useIntegrityGate(errors: ReadonlyArray<unknown>, block: (error: unknown) => void) { useEffect(() => { const failure = errors.find(isIntegrityFailure); if (failure) block(failure); }, [block, ...errors]); }

function OverviewView({ api, sliceApi, provider, campaign, route, revision, navigate, block }: { api: AtlasApi; sliceApi: SliceApi; provider: Resource<ProviderState>; campaign: AtlasCampaignItem; route: AtlasRoute; revision: AtlasRevisionRef; navigate: Navigate; block: (error: unknown) => void }) {
  const query = revisionQuery(revision); const overview = useResource<AtlasOverview>(() => api.overview(campaign.campaign_id, query), [api, campaign.campaign_id, revision.revision_id]); const recent = useResource(() => api.history(campaign.campaign_id, { ...query, limit: 5, direction: "backward" }), [api, campaign.campaign_id, revision.revision_id]); useIntegrityGate([overview.error, recent.error], block);
  return <section aria-busy={overview.pending}><Heading>{overview.value?.campaign_name ?? campaign.campaign_name}</Heading><p className="eyebrow">Adapter: {overview.value?.adapter_id ?? campaign.adapter_id}</p><p><Link href={atlasHref(campaign.campaign_id, "records", { revisionId: revision.revision_id })} navigate={navigate} className="button-link">Browse records</Link></p>{!!overview.error && <ErrorState error={overview.error} retry={overview.retry} />}{overview.value && <><dl className="totals"><div><dt>Records</dt><dd>{overview.value.record_count}</dd></div><div><dt>Relationships</dt><dd>{overview.value.edge_occurrence_count}</dd></div><div><dt>Approved revisions</dt><dd>{overview.value.approved_revision_count}</dd></div></dl><FacetLinks facets={overview.value.facets} campaignId={campaign.campaign_id} revisionId={revision.revision_id} navigate={navigate} /></>}<GroundedAiPanel api={sliceApi} provider={provider} campaign={campaign} route={route} revision={revision} navigate={navigate} /><WorkflowPanels api={api} campaign={campaign} route={route} revision={revision} navigate={navigate} block={block} /><section className="card" aria-labelledby="recent-history-heading" aria-busy={recent.pending}><h2 id="recent-history-heading">Most recent approved revisions</h2>{!!recent.error && <ErrorState error={recent.error} retry={recent.retry} />}{recent.value && <HistoryEntries value={recent.value} campaignId={campaign.campaign_id} navigate={navigate} compact />}</section></section>;
}

function RecordsView({ api, campaign, route, revision, navigate, block }: { api: AtlasApi; campaign: AtlasCampaignItem; route: AtlasRoute; revision: AtlasRevisionRef; navigate: Navigate; block: (error: unknown) => void }) {
  const [draftQuery, setDraftQuery] = useState(route.q); const [creating, setCreating] = useState(false); useEffect(() => setDraftQuery(route.q), [route.q]); const state = stateFromRoute(route, revision.revision_id); const records = useResource<AtlasRecordLibraryResult>(() => api.records(campaign.campaign_id, { ...revisionQuery(revision), q: route.q, types: route.type ? [route.type] : [], authorities: route.authority ? [route.authority] : [], statuses: route.status ? [route.status] : [], ...(route.cursor ? { cursor: route.cursor } : {}) }), [api, campaign.campaign_id, revision.revision_id, route.q, route.type, route.authority, route.status, route.cursor]); useIntegrityGate([records.error], block); const changeState = (next: Partial<AtlasUrlState>) => navigate(atlasHref(campaign.campaign_id, "records", { ...state, ...next, cursor: null })); const submit = (event: FormEvent<HTMLFormElement>) => { event.preventDefault(); changeState({ q: new FormData(event.currentTarget).get("record-search")?.toString().trim() ?? "" }); }; const revisionHasRecords = records.value ? records.value.facets.record_types.some((facet) => facet.count > 0) : true;
  return <section aria-busy={records.pending}><Heading>Records</Heading><button type="button" onClick={() => setCreating((value) => !value)} aria-expanded={creating}>{creating ? "Close create editor" : "Create typed record"}</button>{creating && <RecordEditor campaignId={campaign.campaign_id} revisionId={revision.revision_id} recordId="__new__" navigate={navigate} />}<form className="search-form" role="search" onSubmit={submit}><label htmlFor="record-search">Search campaign records</label><div><input id="record-search" name="record-search" value={draftQuery} onChange={(event) => setDraftQuery(event.target.value)} /><button type="submit">Search</button><button type="button" onClick={() => { setDraftQuery(""); changeState({ q: "" }); }}>Clear</button></div></form><div className="filters"><label>Type<select value={route.type ?? ""} onChange={(event) => changeState({ type: event.target.value || null })}><option value="">All types</option>{records.value?.facets.record_types.map((facet) => <option key={facet.value} value={facet.value}>{titleCase(facet.value)} ({facet.count})</option>)}</select></label><label>Authority<select value={route.authority ?? ""} onChange={(event) => changeState({ authority: (event.target.value || null) as AtlasUrlState["authority"] })}><option value="">All authorities</option>{records.value?.facets.authorities.map((facet) => <option key={facet.value} value={facet.value}>{titleCase(facet.value)} ({facet.count})</option>)}</select></label><label>Status<select value={route.status ?? ""} onChange={(event) => changeState({ status: (event.target.value || null) as AtlasUrlState["status"] })}><option value="">All statuses</option>{records.value?.facets.statuses.map((facet) => <option key={facet.value} value={facet.value}>{statusFilterLabel(facet.value)} ({facet.count})</option>)}</select></label></div>{!!records.error && <ErrorState error={records.error} retry={records.retry} />}{records.value && <><p role="status">{records.value.total} matching {records.value.total === 1 ? "record" : "records"}.</p>{!records.value.items.length ? <p className="empty-state">{revisionHasRecords ? "No records match this search and filter combination." : "No records are stored in this revision."}</p> : <ul className="record-list">{records.value.items.map((record) => <li key={record.record_id} className="card"><h2><Link href={recordHref(campaign.campaign_id, record.record_id, state)} navigate={navigate}>{record.name}</Link></h2><p>{titleCase(record.record_type)}</p><p>{record.summary || "No summary recorded."}</p><RecordLabels authority={record.authority} status={record.raw_status} /></li>)}</ul>}<nav className="pagination" aria-label="Record pages">{records.value.previous_cursor ? <Link href={atlasHref(campaign.campaign_id, "records", { ...state, cursor: records.value.previous_cursor })} navigate={navigate}>Previous</Link> : <span aria-disabled="true">Previous</span>}{records.value.next_cursor ? <Link href={atlasHref(campaign.campaign_id, "records", { ...state, cursor: records.value.next_cursor })} navigate={navigate}>Next</Link> : <span aria-disabled="true">Next</span>}</nav></>}</section>;
}

type RecordConnections = {
  edges: ReadonlyArray<AtlasNeighborhood["edges"][number]>;
  records: ReadonlyMap<string, AtlasNeighborhood["focus"]>;
};

async function loadRecordConnections(api: AtlasApi, campaign: AtlasCampaignItem, recordId: string, revision: AtlasRevisionRef): Promise<RecordConnections> {
  const edges: AtlasNeighborhood["edges"][number][] = [];
  const records = new Map<string, AtlasNeighborhood["focus"]>();
  const edgeIds = new Set<string>();
  const cursors = new Set<string>();
  let cursor: string | undefined;
  let total: number | null = null;
  do {
    const page = await api.neighborhood(campaign.campaign_id, recordId, { ...revisionQuery(revision), ...(cursor ? { cursor } : {}) });
    const pageRecords = new Map([[page.focus.record_id, page.focus], ...page.neighbors.map((item) => [item.record_id, item] as const)]);
    const verified = bindingMatches(page.binding, campaign.campaign_id, revision)
      && page.focus.record_id === recordId
      && page.focus.content_digest === (records.get(recordId)?.content_digest ?? page.focus.content_digest)
      && (total === null || total === page.total_edges)
      && page.edges.every((edge) => /^edge_[0-9a-f]{64}$/.test(edge.edge_id) && !edgeIds.has(edge.edge_id) && pageRecords.has(edge.source_record_id) && pageRecords.has(edge.target_record_id));
    if (!verified) throw new Error("unverified_relationship_data");
    total ??= page.total_edges;
    records.set(page.focus.record_id, page.focus);
    page.neighbors.forEach((item) => records.set(item.record_id, item));
    page.edges.forEach((edge) => { edgeIds.add(edge.edge_id); edges.push(edge); });
    if (page.next_cursor && cursors.has(page.next_cursor)) throw new Error("repeated_relationship_cursor");
    cursor = page.next_cursor ?? undefined;
    if (cursor) cursors.add(cursor);
  } while (cursor);
  if (edges.length !== total) throw new Error("incomplete_relationship_data");
  return {
    edges: edges.filter((edge) => edge.source_record_id === recordId).sort((left, right) => left.occurrence_order - right.occurrence_order || left.edge_id.localeCompare(right.edge_id)),
    records,
  };
}

function LinkedConnectionContext({ context, relatedName, href, navigate }: { context: string; relatedName: string; href: string; navigate: Navigate }) {
  const offset = context.indexOf(relatedName);
  if (offset >= 0) return <>{context.slice(0, offset)}<Link href={href} navigate={navigate}>{relatedName}</Link>{context.slice(offset + relatedName.length)}</>;
  return <>{context} <span>Related record: </span><Link href={href} navigate={navigate}>{relatedName}</Link>.</>;
}

function RecordConnectionStatements({ resource, campaign, route, revision, contentDigest, navigate }: { resource: Resource<RecordConnections>; campaign: AtlasCampaignItem; route: AtlasRoute; revision: AtlasRevisionRef; contentDigest: string; navigate: Navigate }) {
  if (resource.pending) return <p role="status">Loading connections.</p>;
  if (resource.error || !resource.value || resource.value.records.get(route.recordId ?? "")?.content_digest !== contentDigest) return <div className="error atlas-error" role="alert"><p>Connection details are unavailable for this revision.</p><button type="button" onClick={resource.retry}>Retry</button></div>;
  if (!resource.value.edges.length) return <p>No explicit connections in this record.</p>;
  const state = stateFromRoute(route, revision.revision_id);
  return <ul className="record-connection-list">{resource.value.edges.map((edge) => {
    const self = edge.target_record_id === edge.source_record_id;
    const related = resource.value!.records.get(edge.target_record_id);
    if (!related) return null;
    const href = recordHref(campaign.campaign_id, related.record_id, { ...state, relationshipCursor: null, generationCursor: null, proposalCursor: null });
    return <li key={edge.edge_id}><LinkedConnectionContext context={edge.context} relatedName={related.name} href={href} navigate={navigate} />{self && <span className="connection-self-label"> This record.</span>}</li>;
  })}</ul>;
}

function RecordDetailContent({ api, sliceApi, provider, campaign, route, revision, navigate, block }: { api: AtlasApi; sliceApi: SliceApi; provider: Resource<ProviderState>; campaign: AtlasCampaignItem; route: AtlasRoute; revision: AtlasRevisionRef; navigate: Navigate; block: (error: unknown) => void }) {
  const recordId = route.recordId ?? ""; const query = revisionQuery(revision); const detail = useResource(() => api.record(campaign.campaign_id, recordId, query), [api, campaign.campaign_id, recordId, revision.revision_id]); const recordConnections = useResource(() => loadRecordConnections(api, campaign, recordId, revision), [api, campaign.campaign_id, recordId, revision.revision_id]); const neighborhood = useResource(() => api.neighborhood(campaign.campaign_id, recordId, { ...query, ...(route.relationshipCursor ? { cursor: route.relationshipCursor } : {}) }), [api, campaign.campaign_id, recordId, revision.revision_id, route.relationshipCursor]); const history = useResource(() => api.history(campaign.campaign_id, { ...query, subject_record_id: recordId, limit: 50 }), [api, campaign.campaign_id, recordId, revision.revision_id]); useIntegrityGate([detail.error, recordConnections.error, neighborhood.error, history.error], block); const state = stateFromRoute(route, revision.revision_id);
  const readableConnections = detail.value ? <RecordConnectionStatements resource={recordConnections} campaign={campaign} route={route} revision={revision} contentDigest={detail.value.record.content_digest} navigate={navigate} /> : null;
  return <section aria-busy={detail.pending}><p><Link href={atlasHref(campaign.campaign_id, "records", state)} navigate={navigate}>Back to records</Link></p><Heading focusKey={detail.value?.record.record_id}>{detail.value?.record.name ?? "Record detail"}</Heading>{!!detail.error && <ErrorState error={detail.error} retry={detail.retry} />}{detail.value && <><p className="eyebrow">{titleCase(detail.value.record.record_type)}</p><RecordLabels authority={detail.value.record.authority} status={detail.value.record.raw_status} /><p>{detail.value.record.summary || "No summary recorded."}</p><section className="card source-binding" aria-labelledby="source-binding-heading"><h2 id="source-binding-heading">Source binding</h2><dl><div><dt>Record ID</dt><dd><code>{detail.value.record.record_id}</code></dd></div><div><dt>Revision</dt><dd><code>{detail.value.binding.viewed_revision.revision_id}</code></dd></div><div><dt>Normalized content digest</dt><dd><code>{detail.value.record.content_digest}</code></dd></div></dl></section><section className="card" aria-labelledby="record-content-heading"><h2 id="record-content-heading">Record content</h2><SafeMarkdown source={detail.value.record.content} connections={readableConnections} /><details><summary>Exact source text</summary><pre>{detail.value.record.content}</pre></details></section><GroundedAiPanel api={sliceApi} provider={provider} campaign={campaign} route={route} revision={revision} navigate={navigate} record={detail.value.record} /><WorkflowPanels api={api} campaign={campaign} route={route} revision={revision} record={detail.value.record} navigate={navigate} block={block} /></>}<section className="card relationships" aria-labelledby="relationship-heading" aria-busy={neighborhood.pending}><h2 id="relationship-heading">Relationships</h2>{neighborhood.pending && <p>Loading relationships.</p>}{!!neighborhood.error && <ErrorState error={neighborhood.error} retry={neighborhood.retry}>{isCursorFailure(neighborhood.error) && <button type="button" onClick={() => navigate(recordHref(campaign.campaign_id, recordId, { ...state, relationshipCursor: null }))}>Start at newest</button>}</ErrorState>}{neighborhood.value && <RelationshipView value={neighborhood.value} campaign={campaign} route={route} revision={revision} navigate={navigate} />}</section><section className="card" aria-labelledby="record-history-heading" aria-busy={history.pending}><h2 id="record-history-heading">Approved history for this record</h2>{!!history.error && <ErrorState error={history.error} retry={history.retry} />}{history.value && <HistoryEntries value={history.value} campaignId={campaign.campaign_id} navigate={navigate} />}</section></section>;
}

function HistoryView({ api, campaign, revision, route, navigate, block }: { api: AtlasApi; campaign: AtlasCampaignItem; revision: AtlasRevisionRef; route: AtlasRoute; navigate: Navigate; block: (error: unknown) => void }) { const history = useResource(() => api.history(campaign.campaign_id, { ...revisionQuery(revision), limit: 50, ...(route.cursor ? { cursor: route.cursor } : {}) }), [api, campaign.campaign_id, revision.revision_id, route.cursor]); useIntegrityGate([history.error], block); const state = stateFromRoute(route, revision.revision_id); return <section aria-busy={history.pending}><Heading>Approved history</Heading><p>Only verified snapshot revisions appear here.</p>{!!history.error && <ErrorState error={history.error} retry={history.retry} />}{history.value && <><p role="status">{history.value.total} approved revision {history.value.total === 1 ? "event" : "events"}.</p><HistoryEntries value={history.value} campaignId={campaign.campaign_id} navigate={navigate} /><nav className="pagination" aria-label="History pages">{history.value.previous_cursor ? <Link href={atlasHref(campaign.campaign_id, "history", { ...state, cursor: history.value.previous_cursor })} navigate={navigate}>Previous</Link> : <span aria-disabled="true">Previous</span>}{history.value.next_cursor ? <Link href={atlasHref(campaign.campaign_id, "history", { ...state, cursor: history.value.next_cursor })} navigate={navigate}>Next</Link> : <span aria-disabled="true">Next</span>}</nav></>}</section>; }

export function AtlasApp({ api, sliceApi = httpSliceApi, readiness, location, navigate }: { api: AtlasApi; sliceApi?: SliceApi; readiness: () => Promise<ProviderState>; location: string; navigate: Navigate }) {
  const route = useMemo(() => parseAtlasRoute(location), [location]); const previousLocation = useRef(location); const campaigns = useResource(() => api.campaigns(), [api]); const provider = useResource(() => readiness(), [readiness]); const [integrityError, setIntegrityError] = useState<unknown>(null); const campaign = campaigns.value?.campaigns.find((item) => item.campaign_id === route.campaignId) ?? null; const requestedRevision = route.revisionId ?? campaign?.head_revision.revision_id ?? null; const knownRevision = campaign && requestedRevision === campaign.head_revision.revision_id ? campaign.head_revision : campaign?.projected_revision?.revision_id === requestedRevision ? campaign.projected_revision : null; const revisionKey = campaign && requestedRevision ? `${campaign.campaign_id}\u0000${requestedRevision}` : null; const resolved = useResource(campaign && requestedRevision && !knownRevision && revisionKey ? () => api.resolveRevision(campaign.campaign_id, requestedRevision).then((revision) => ({ key: revisionKey, revision })) : null, [api, campaign?.campaign_id, requestedRevision, revisionKey]); const viewed = knownRevision ?? (resolved.value?.key === revisionKey ? resolved.value.revision : null);
  useEffect(() => setIntegrityError(null), [route.campaignId, requestedRevision]); useEffect(() => { if (previousLocation.current !== location) document.querySelector<HTMLElement>("#atlas-content h1")?.focus(); previousLocation.current = location; }, [location]); useEffect(() => { if (campaign && !route.revisionId) navigate(atlasHref(campaign.campaign_id, route.kind === "records" ? "records" : route.kind === "history" ? "history" : "overview", { revisionId: campaign.head_revision.revision_id }), true); }, [campaign, route.kind, route.revisionId, navigate]);
  const bare = (content: ReactNode) => <div className="app-shell"><a className="skip-link" href="#main-content">Skip to main content</a><header className="banner"><strong>Warden Drydock</strong><ProviderStatus resource={provider} /></header><main id="main-content" tabIndex={-1}>{content}</main></div>;
  if (campaigns.error) return bare(<><Heading>Campaign Atlas</Heading><ErrorState error={campaigns.error} retry={campaigns.retry} /></>); if (!campaigns.value) return bare(<section aria-busy="true"><Heading>Loading Campaign Atlas</Heading></section>); if (!campaigns.value.campaigns.length) return bare(<><Heading>No campaign yet</Heading><p>Create a campaign in the proposal workspace first.</p><Link href="/" navigate={navigate}>Open proposal workspace</Link></>); if (!campaign) return bare(<><Heading>Campaign unavailable</Heading><p>The requested campaign could not be opened.</p></>);
  if (campaign.recovery_state !== "ready") return <Shell campaign={campaign} route={route} viewed={campaign.head_revision} provider={provider} navigate={navigate}><Heading>Campaign view unavailable</Heading><div className="error" role="alert">{campaign.recovery_state === "integrity_blocked" ? "Snapshot integrity verification blocks this campaign." : "This campaign projection must be rebuilt before Atlas can open it."}</div></Shell>; if (route.kind === "invalid") return <Shell campaign={campaign} route={{ ...route, kind: "overview" }} viewed={campaign.head_revision} provider={provider} navigate={navigate}><Heading>Page unavailable</Heading></Shell>; if (resolved.error) return <Shell campaign={campaign} route={route} viewed={campaign.head_revision} provider={provider} navigate={navigate}><Heading>Revision unavailable</Heading><Link href={openHeadHref(route, campaign.head_revision)} navigate={navigate}>Open head</Link></Shell>; if (!viewed) return <Shell campaign={campaign} route={route} viewed={campaign.head_revision} provider={provider} navigate={navigate}><section aria-busy="true"><Heading>Resolving revision</Heading></section></Shell>; if (integrityError) return <Shell campaign={campaign} route={route} viewed={viewed} provider={provider} navigate={navigate}><Heading>Campaign view blocked</Heading><ErrorState error={integrityError} retry={() => { setIntegrityError(null); campaigns.retry(); }} /></Shell>;
  return <Shell campaign={campaign} route={route} viewed={viewed} provider={provider} navigate={navigate}>{route.kind === "overview" && <OverviewView api={api} sliceApi={sliceApi} provider={provider} campaign={campaign} route={route} revision={viewed} navigate={navigate} block={setIntegrityError} />}{route.kind === "records" && <RecordsView api={api} campaign={campaign} route={route} revision={viewed} navigate={navigate} block={setIntegrityError} />}{route.kind === "record" && <RecordDetailView api={api} sliceApi={sliceApi} provider={provider} campaign={campaign} route={route} revision={viewed} navigate={navigate} block={setIntegrityError} />}{route.kind === "history" && <HistoryView api={api} campaign={campaign} revision={viewed} route={route} navigate={navigate} block={setIntegrityError} />}</Shell>;
}
